<UserControl x:Class="wpfOs.View.Apps.Browser"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             xmlns:local="clr-namespace:wpfOs.View.Apps"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <TextBox x:Name="UrlBox" Text="{Binding BrowserUrl, Mode=TwoWay}" 
                Grid.Row="0" Grid.Column="0" Margin="0,0,6,0"/>
        <Button Content="Go" Command="{Binding ChangeWebpageCommand}"
                Grid.Row="0" Grid.Column="1" Width="30"/>

        <wv2:WebView2 Name="webView" Source="https://www.microsoft.com"
                      Grid.Row="1" Grid.ColumnSpan="2" Margin="0,6,0,0"/>
    </Grid>
</UserControl>
