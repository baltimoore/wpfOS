<UserControl x:Class="wpfOs.View.Apps.Browser"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             xmlns:local="clr-namespace:wpfOs.View.Apps"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Button Command="{Binding GoHistoryBackCommand}"
                    Grid.Column="0" Width="30" Height="20" >
                <TextBlock Text="⇦" FontSize="32" VerticalAlignment="Center" FontWeight="Bold" Margin="0,-8,0,0"/>
            </Button>
            <Button Command="{Binding GoHistoryForwardCommand}"
                    Grid.Column="1" Width="30" Height="20" >
                <TextBlock Text="⇨" FontSize="32" VerticalAlignment="Center" FontWeight="Bold" Margin="0,-8,0,0"/>
            </Button>
            <Button Command="{Binding GoHomepageCommand}"
                    Grid.Column="2" Width="30" Height="20" Margin="6,0,0,0">
                <TextBlock Text="⌂" FontSize="20" VerticalAlignment="Center"/>
            </Button>
            
            <TextBox x:Name="UrlBox" Text="{Binding BrowserUrl}" 
                    Grid.Column="3" Margin="6,0,6,0"/>
            
            <Button Content="Go" Command="{Binding ChangeWebpageCommand}"
                    Grid.Column="4" Width="50"/>
        </Grid>

        <wv2:WebView2 Name="webView" Source="{Binding BrowserUrl}"
                      Grid.Row="1" Margin="0,6,0,0"/>
    </Grid>
</UserControl>
